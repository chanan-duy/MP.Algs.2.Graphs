<Window x:Class="Wpf.Fractals.Sierpinski.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Fractals Sierpinski" Height="800" Width="1400" MinHeight="450" MinWidth="700"
        Background="#FF252526" Foreground="WhiteSmoke">
    <Window.Resources>
        <SolidColorBrush x:Key="ControlBackgroundBrush" Color="#FF3E3E42" />
        <SolidColorBrush x:Key="ControlBorderBrush" Color="#FF555555" />
        <SolidColorBrush x:Key="ControlMouseOverBrush" Color="#FF4F4F53" />
        <SolidColorBrush x:Key="ControlPressedBrush" Color="#FF007ACC" />
        <SolidColorBrush x:Key="TextBrush" Color="WhiteSmoke" />
        <SolidColorBrush x:Key="StatusBarBrush" Color="#FF007ACC" />

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource ControlMouseOverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource ControlPressedBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource ControlBackgroundBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="5" />
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="{StaticResource ControlBorderBrush}" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Margin" Value="0,3" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Padding="10" BorderBrush="#FF3F3F46" BorderThickness="0,0,1,0">
            <StackPanel>
                <TextBlock Text="Settings" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" />
                <GroupBox Header="Algorithm" Margin="0,0,0,10">
                    <StackPanel>
                        <RadioButton Name="RecursiveMethodRadio" Content="Recursive" IsChecked="True"
                                     Checked="Algorithm_SelectionChanged" />
                        <RadioButton Name="ChaosGameMethodRadio" Content="Chaos game"
                                     Checked="Algorithm_SelectionChanged" />
                    </StackPanel>
                </GroupBox>
                <TextBlock Name="ParameterLabel" Text="Depth:" Margin="0,10,0,5" />
                <TextBox Name="ParameterTextBox" Text="6" />
                <Button Name="DrawButton" Content="Draw" Click="DrawFractal_Click" Margin="0,20,0,0" />
            </StackPanel>
        </Border>

        <Grid Grid.Column="1">
            <Image Name="FractalImage" Stretch="Uniform" />
            <StatusBar VerticalAlignment="Bottom" Background="{StaticResource StatusBarBrush}">
                <StatusBarItem>
                    <TextBlock Name="StatusTextBlock" Foreground="{StaticResource TextBrush}" />
                </StatusBarItem>
            </StatusBar>
        </Grid>
    </Grid>
</Window>
